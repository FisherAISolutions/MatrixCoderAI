#!/usr/bin/env node

const fixtureIds = [
  'simple-business-website',
  'crud-saas-dashboard',
  'children-story-platform',
];
const baseUrl = process.env.MATRIX_CODER_APP_BASE_URL || 'http://localhost:3000';

console.log('Matrix Coder AI Private Beta Benchmark Certification');
console.log('No provider calls are made by this command.');
console.log('');
console.log('Fixed live limits:');
console.log('- tasks: 14');
console.log('- AI requests: 20');
console.log('- automatic repairs per task: 1');
console.log('- duration: 30 minutes');
console.log('- files: 80');
console.log('- generated bytes: 450000');
console.log('');

for (const fixtureId of fixtureIds) {
  console.log(`Fixture: ${fixtureId}`);
  console.log(
    [
      `$env:MATRIX_CODER_LIVE_BENCHMARK_ID='${fixtureId}'`,
      "$env:MATRIX_CODER_LIVE_BENCHMARK_CONFIRM='RUN_LIVE_ENGINEERING_BENCHMARK'",
      "$env:MATRIX_CODER_LIVE_BENCHMARK_PROVIDER='1'",
      `$env:MATRIX_CODER_APP_BASE_URL='${baseUrl}'`,
      'node scripts/engineering-benchmark-live.cjs',
    ].join('; ')
  );
  console.log('');
}
